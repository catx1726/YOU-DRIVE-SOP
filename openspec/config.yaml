schema: spec-driven

# Project context (High-density SOP 2.0 alignment)
context: |
  This project follows the YOU-DRIVE-SOP framework.
  - Foundry Root: {{FOUNDRY_ROOT}}
  - Security Mandate: Every file modification (CREATE, UPDATE, DELETE) MUST be executed through 'meta-safe-executor'.
  - Distillation Mandate: Identify and extract logic patterns to '.gemini/distill_stage/' before archiving.

# Per-artifact rules
rules:
  tasks:
    - Each implementation task MUST include a 'Validation' step.
    - Reference the 'meta-safe-executor' skill for any destructive operations.
  proposal:
    - Include a 'Potential Assets for Foundry' section to prep for distillation.
  design:
    - Clearly separate 'Business Logic' from 'Reusable Patterns'.

# Skill Linkage (Ensuring sub-repo accessibility)
skills:
  safety: .gemini/skills/meta-safe-executor/SKILL.md
  distiller: .gemini/skills/meta-distiller/SKILL.md
  debugging: .gemini/skills/systematic-debugging/SKILL.md
