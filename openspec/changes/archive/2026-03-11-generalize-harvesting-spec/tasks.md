# Tasks

## 1. 规约更新 (Spec Refinement)
- [x] 将 `openspec/changes/generalize-harvesting-spec/specs/foundry-protocols/spec.md` 中的修改合并至 `openspec/specs/foundry-protocols/spec.md`。
- [x] 确保 `Scenario: 资产去业务化收割` 中的描述已更新为通用术语。

## 2. 演进决策固化 (Decision Sync)
- [x] 创建 ADR `002-generalize-harvesting-examples.md`，记录本次修正的背景与架构考量。

## 3. 归档 (Archive)
- [x] 使用 `/opsx:archive` 归档本次变更。
